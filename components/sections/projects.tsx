"use client";
import { motion } from "framer-motion";
import { ArrowUpRight } from "@/components/icons";
import { Reveal } from "@/components/reveal";
import { projects } from "@/lib/data";
const themes = { orange: "from-orange-400/80 via-orange-200 to-[#fff0e9] dark:from-orange-500/40 dark:to-[#1c2433]", blue: "from-sky-500/70 via-sky-200 to-[#e9f6ff] dark:from-sky-500/40 dark:to-[#1c2433]", violet: "from-violet-500/70 via-violet-200 to-[#f3edff] dark:from-violet-500/40 dark:to-[#1c2433]" };
export function Projects() { return <section id="projects" className="border-y border-[var(--line)] py-24"><div className="shell"><Reveal><p className="eyebrow">03 / Selected work</p><div className="mt-4 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><h2 className="section-title">Making complexity<br />feel considered.</h2><p className="max-w-xs text-sm leading-6 text-[var(--muted)]">A selection of systems built for people doing meaningful work.</p></div></Reveal><div className="mt-12 grid gap-5 md:grid-cols-3">{projects.map((project, index) => <Reveal key={project.title} delay={index * .08}><motion.article whileHover={{ y: -7 }} transition={{ type: "spring", stiffness: 330, damping: 22 }} className="group h-full overflow-hidden rounded-2xl border border-[var(--line)] bg-[var(--paper)]"><div className={`relative h-48 overflow-hidden bg-gradient-to-br ${themes[project.accent]}`}><div className="absolute inset-6 rounded-xl border border-white/50 bg-white/25 p-4 backdrop-blur-sm"><div className="h-2 w-16 rounded-full bg-white/65" /><div className="mt-5 h-2 w-4/5 rounded-full bg-white/55" /><div className="mt-3 h-2 w-3/5 rounded-full bg-white/45" /><div className="absolute bottom-4 left-4 right-4 flex gap-2"><span className="h-12 flex-1 rounded-lg bg-white/35" /><span className="h-12 w-12 rounded-lg bg-white/60" /></div></div><span className="absolute right-4 top-4 font-mono text-xs font-bold text-white/80">0{index + 1}</span></div><div className="p-6"><p className="font-mono text-xs uppercase tracking-wider text-[var(--accent)]">{project.type}</p><h3 className="mt-2 text-xl font-semibold tracking-tight">{project.title}</h3><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tags.map((tag) => <span key={tag} className="rounded-full bg-[var(--ink)]/[.06] px-2.5 py-1 font-mono text-[10px] text-[var(--muted)] dark:bg-white/[.08]">{tag}</span>)}</div><span className="mt-6 inline-flex items-center gap-1 text-sm font-semibold text-[var(--ink)] transition group-hover:text-[var(--accent)]">Case study <ArrowUpRight className="size-4" /></span></div></motion.article></Reveal>)}</div></div></section>; }
