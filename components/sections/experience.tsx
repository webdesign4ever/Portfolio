import { Reveal } from "@/components/reveal";
import { experiences } from "@/lib/data";
export function Experience() { return <section id="experience" className="shell py-24"><Reveal><p className="eyebrow">02 / Experience</p><h2 className="section-title mt-4">Work with real-world weight.</h2></Reveal><div className="mt-14 border-l border-[var(--line)] pl-6 sm:pl-10">{experiences.map((item, index) => <Reveal key={item.company} delay={index * .08} className="relative pb-12 last:pb-0"><span className="absolute -left-[31px] top-1.5 size-3 rounded-full border-[3px] border-[var(--paper)] bg-[var(--accent)] sm:-left-[47px]" /><div className="grid gap-4 lg:grid-cols-[1fr_2fr]"><div><p className="font-mono text-xs uppercase tracking-wider text-[var(--accent)]">{item.period}</p><h3 className="mt-2 text-xl font-semibold tracking-tight">{item.role}</h3><p className="mt-1 text-sm text-[var(--muted)]">{item.company} · {item.location}</p></div><ul className="space-y-3 text-sm leading-6 text-[var(--muted)]">{item.summary.map((line) => <li key={line} className="flex gap-3"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-[var(--accent)]" />{line}</li>)}</ul></div></Reveal>)}</div></section>; }
