"use client";
import { FormEvent, useState } from "react";
import { ArrowUpRight, Github, Linkedin, Mail, MapPin, Phone, Send } from "@/components/icons";
import { profile } from "@/lib/data";
type Status = { kind: "idle" | "success" | "error"; message: string };
export function Contact() {
  const [status, setStatus] = useState<Status>({ kind: "idle", message: "" }); const [sending, setSending] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = Object.fromEntries(new FormData(form)); if (!String(data.name).trim() || !String(data.email).includes("@") || String(data.message).trim().length < 10) { setStatus({ kind: "error", message: "Please provide your name, a valid email, and a message of at least 10 characters." }); return; } setSending(true); setStatus({ kind: "idle", message: "" }); try { const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Could not send your message."); setStatus({ kind: "success", message: "Thanks - your message is on its way." }); form.reset(); } catch (error) { setStatus({ kind: "error", message: error instanceof Error ? error.message : "Could not send your message. Please email me directly." }); } finally { setSending(false); } }
  const field = "focus-ring w-full rounded-xl border border-[#dce2ea] bg-transparent px-4 py-3 text-sm text-[#14213d] placeholder:text-[#64748b]/70 transition focus:border-[#ff5b36] dark:border-[#43516a] dark:text-[#f8f7f4] dark:placeholder:text-[#a5b2c8]/70 dark:focus:border-[#ff7959]";
  return <section id="contact" className="shell py-24"><div className="rounded-3xl border border-[#dce2ea] bg-white px-6 py-12 text-[#14213d] shadow-sm sm:px-10 lg:px-14 dark:border-0 dark:bg-[#14213d] dark:text-[#f8f7f4] dark:shadow-none"><div className="grid gap-12 lg:grid-cols-[.9fr_1.1fr]"><div><p className="eyebrow">06 / Contact</p><h2 className="mt-4 text-4xl font-semibold tracking-[-.055em] sm:text-5xl">Have a project in mind?</h2><p className="mt-6 max-w-md leading-7 text-[#64748b] dark:text-[#f8f7f4]/65">I&apos;m open to discussing product work, front-end roles, and interesting technical problems.</p><div className="mt-10 space-y-3 text-sm text-[#64748b] dark:text-[#f8f7f4]/70"><a className="focus-ring flex items-center gap-3 hover:text-[#ff5b36] dark:hover:text-[#ff7959]" href={`mailto:${profile.email}`}><Mail className="size-4" />{profile.email}</a><p className="flex items-center gap-3"><MapPin className="size-4" />{profile.location}</p><a className="focus-ring flex items-center gap-3 hover:text-[#ff5b36] dark:hover:text-[#ff7959]" href={`tel:${profile.phone.replace(/-/g, "")}`}><Phone className="size-4" />{profile.phone}</a><div className="flex gap-4 pt-2"><a className="focus-ring hover:text-[#ff5b36] dark:hover:text-[#ff7959]" href={profile.github} target="_blank" rel="noreferrer" aria-label="GitHub"><Github className="size-5" /></a><a className="focus-ring hover:text-[#ff5b36] dark:hover:text-[#ff7959]" href={profile.linkedin} target="_blank" rel="noreferrer" aria-label="LinkedIn"><Linkedin className="size-5" /></a></div></div></div><form onSubmit={submit} className="space-y-4" noValidate><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm text-[#475569] dark:text-[#f8f7f4]/75">Name<input name="name" required autoComplete="name" placeholder="Your name" className={`${field} mt-2`} /></label><label className="text-sm text-[#475569] dark:text-[#f8f7f4]/75">Email<input name="email" required type="email" autoComplete="email" placeholder="you@example.com" className={`${field} mt-2`} /></label></div><label className="block text-sm text-[#475569] dark:text-[#f8f7f4]/75">Message<textarea name="message" required minLength={10} rows={5} placeholder="Tell me a little about your project..." className={`${field} mt-2 resize-y`} /></label>{status.kind !== "idle" && <p role="status" className={`text-sm ${status.kind === "success" ? "text-emerald-600 dark:text-emerald-300" : "text-orange-600 dark:text-orange-300"}`}>{status.message}</p>}<button disabled={sending} className="focus-ring inline-flex items-center gap-2 rounded-full bg-[#ff5b36] px-5 py-3 text-sm font-semibold text-white transition hover:brightness-110 disabled:cursor-wait disabled:opacity-60">{sending ? "Sending..." : "Send message"} <Send className="size-4" /></button><a href={`mailto:${profile.email}`} className="focus-ring ml-4 inline-flex items-center gap-1 text-sm text-[#64748b] hover:text-[#ff5b36] dark:text-[#f8f7f4]/60 dark:hover:text-[#ff7959]">Email directly <ArrowUpRight className="size-3" /></a></form></div></div></section>;
}
